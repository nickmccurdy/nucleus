<%= form_for(@bin, html: { class: 'pure-form pure-form-stacked' }) do |f| %>
  <fieldset>
    <% if @bin.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@bin.errors.count, "error") %> prohibited this bin from being saved:</h2>

        <ul>
        <% @bin.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.label :title %>
    <%= f.text_field :title %>

    <%= f.label :description %>
    <%= f.text_area :description %>

    <%= f.label :body %>
    <%= f.text_area :body %>

    <%= f.submit class: "pure-button pure-button-primary" %>
  </fieldset>
<% end %>
