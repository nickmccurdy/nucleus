= form_for(@bin) do |f|
  - if @bin.errors.any?
    #error_explanation
      h2 #{ pluralize(@bin.errors.count, "error") } prohibited this bin from being saved:
      ul
        - @bin.errors.full_messages.each do |message|
          li= message
  .field
    = f.label :title
    br
    = f.text_field :title
  .field
    = f.label :description
    br
    = f.text_area :description
  .field
    = f.label :body
    br
    = f.text_area :body
  .actions
    = f.submit
